# Frontend environment template — copy to .env.local for dev overrides.
# Vite only exposes variables prefixed with VITE_ to the client bundle.

# Base URL the SPA calls. Defaults to http://localhost:8000 in src/api.ts
# when unset. In production this should point to your ALB or API Gateway.
VITE_API_URL=http://localhost:8000
