# EDA notebook dependencies — install with:
#   pip install -r eda/requirements.txt
#
# For the FastAPI backend deps see api/pyproject.toml.

pandas>=2.2,<4
numpy>=1.26,<3
matplotlib>=3.8,<4
scipy>=1.12,<2

# Notebook runtime + headless execution
jupyter>=1.0
ipykernel>=6.29
nbconvert>=7.16
